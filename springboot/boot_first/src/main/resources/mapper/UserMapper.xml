<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.evan.dao.UserDao">

    <select id="getUserById" resultType="com.evan.domain.User">
        select *
        from my_jdbc.user
        where id = #{id}
    </select>
    <select id="getAllUsers" resultType="com.evan.domain.User">
        select *
        from my_jdbc.user;
    </select>
</mapper>